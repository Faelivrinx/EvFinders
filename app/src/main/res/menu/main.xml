<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/action_mainActivity_cancel_create"
        android:title="@string/remove"
        app:showAsAction="always"
        android:icon="@drawable/quantum_ic_clear_white_24"
        />

    <item android:id="@+id/action_mainActivity_add_event"
        android:title="@string/remove"
        app:showAsAction="always"
        android:icon="@mipmap/ic_add"
        />

    <item android:id="@+id/action_mainActivity_back"
        android:title="@string/remove"
        app:showAsAction="always"
        android:icon="@mipmap/ic_white_back"
        />

    <item android:id="@+id/action_mainActivity_accept_event"
        android:title="@string/remove"
        app:showAsAction="always"
        android:icon="@mipmap/ic_accept"
        />

</menu>