<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_us" android:icon="@mipmap/ic_friends"
            android:title="Friends" />
        <item android:id="@+id/nav_events" android:icon="@android:drawable/ic_dialog_dialer"
            android:title="Events" />
    </group>

    <item android:title="Management">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/btn_star"
                android:title="About us" />
            <item android:id="@+id/nav_logout" android:icon="@android:drawable/ic_menu_send"
                android:title="Logout" />
        </menu>
    </item>

</menu>